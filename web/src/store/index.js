import { createStore } from 'vuex'
import ModuleModel from "@/store/model" 

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    model: ModuleModel
  }
})
